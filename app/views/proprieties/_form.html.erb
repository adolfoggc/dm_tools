<%= form_with(model: propriety) do |form| %>
  <% if propriety.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(propriety.errors.count, "error") %> prohibited this propriety from being saved:</h2>

      <ul>
        <% propriety.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_field :description %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
